---
export interface Props {
  padding?: 'default' | 'lg'
  textAlign?: 'left' | 'center'
  children?: any
}

const { padding = 'default', textAlign = 'left', children } = Astro.props
---

<div class={`glass-card card-pad${padding === 'lg' ? '-lg' : ''} card-align-${textAlign}`}>
  <slot />
</div>
